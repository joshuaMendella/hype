# Hype — Build Changelog

Session-by-session record of what was built and fixed. Evergreen project context, architecture, rules, and current focus live in [CLAUDE.md](CLAUDE.md).

## What's been built (as of 2026-07-07, updated session 20)
- [x] Monorepo scaffold (Turborepo, pnpm, TypeScript)
- [x] Next.js app scaffolded in apps/web
- [x] Supabase schema: profiles, vault_notes, vault_links, conversations, messages, extractions
- [x] RLS policies + triggers (auto-create profile + root vault note on signup)
- [x] Auth middleware (protects /graph, redirects unauthenticated)
- [x] Login + Signup pages (/login, /signup)
- [x] Graph page (/graph) — full-screen D3 force graph + conversational UI overlay
- [x] GraphCanvas component — Obsidian-style, topic colors, zoom/pan/drag, tooltips, zoom capped at 1.5x
- [x] ChatPanel component — full-screen conversational UI (AI top-center, input bottom-center, typewriter reveal, Poppins font)
- [x] GraphWrapper component — thin client wrapper sharing refreshTrigger state between GraphCanvas and ChatPanel
- [x] /api/chat — Cerebras gpt-oss-120b interviewer, vault context, known-facts, today's events, drill-down, brand rule, deflection, agenda injection
- [x] lib/ai/extract.ts — entity-centric extraction (Topic → Brand → Item or Topic → Entity), no You→Topic links, agenda update after each run
- [x] lib/ai/checklists.ts — required attributes per topic, Agenda type, CHECKLIST_PROMPT for system prompt
- [x] lib/ai/topics.ts — 31 topics
- [x] lib/ai/categories.ts — topic → subcategories map (still present, not used in graph routing)
- [x] Supabase: conversations.agenda JSONB — tracks current entity + pending threads across turns
- [x] Graph updates — event-driven (fires once 4s after each chat reply)
- [x] Supabase: reset_vault(user_id) function — idempotent vault wipe, re-seeds "You" root at _profile.md
- [x] Supabase: scheduled_for date column on vault_notes + partial index for today's event queries
- [x] HYPE_BUSINESS_ASSESSMENT.md — business memo: consent-based ad model, unit economics, next steps
- [x] Single-model extraction — 70B (Cerebras gpt-oss-120b) returns JSON with reply + extraction data in one call; 8B Groq call eliminated from hot path
- [x] extractFacts() decoupled from LLM — receives pre-extracted data, handles only agenda management + vault writes
- [x] Chat provider switched to Cerebras (free tier, gpt-oss-120b, 120B params, reasoning model)
- [x] Interviewer persona redesigned (session 7) — new SYSTEM_PROMPT: banned hollow phrases, ≤12-word reaction cap, numbered focus priority, explicit memory-check rule, conversational drill-down phrasing, concrete dead-end thresholds, 3 session-end triggers, 4 input-handling cases (off-topic/emotional/jailbreak/sensitive), honest AI identity
- [x] ONBOARDING_PROMPT — 4-step first-conversation intro, waits for user ack at each step, `onboarding_complete` signal marks profile as onboarded
- [x] profiles.onboarded BOOLEAN — gates onboarding flow; set false → onboarding, set true → normal interview
- [x] /hypereset skill — wipes vault, clears agenda, resets onboarded flag in one command
- [x] Onboarding flow fixed (session 8) — ChatPanel now fetches opening from API on mount (messages: []); removed hardcoded opening() that bypassed server; route accepts empty messages array
- [x] Onboarding skip-ahead rule tightened — "let's do it" now advances to next step, not Step 5; only explicit "skip this" / "just start already" jumps ahead
- [x] JSON leak fix — model sometimes outputs text before JSON block; regex fallback extracts and retries the JSON block, preventing silent extraction failure
- [x] Attribute bleed fix — entity-level attributes now nested inside extraction.entities[].attributes; top-level extraction.attributes is drill-down only (existing agenda entity); makeAgendaItem seeds attributes + missing from entity-level attrs
- [x] Topic name enforcement — extraction prompt now inlines full topic enum instead of soft "from the list above" reference; prevents free-form topics like "Place visited"
- [x] City/hometown detection — interviewer drill-down rule added: "my city X" → AI confirms as home city, captured as Location entity
- [x] Typewriter speed slowed (52ms → 85ms per word)
- [x] Dynamic font size in ChatPanel — scales down for longer AI messages (>100 chars, >200 chars breakpoints)
- [x] /hypesave skill — end-of-session commit + push command
- [x] Hype__Suggestions.docx reviewed — design proposals for extraction quality, graph structure, advertiser profile (session 9)
- [x] lib/ai/entityTypes.ts — 5 entity types (item/brand/place/person/event) with tier 1/2/3 parameter stacks; tier 1 completion unlocks vault write (PR #1)
- [x] checklists.ts refactored — AgendaItem gains weight, tier1_complete, tags fields; CHECKLIST_PROMPT rewritten around tiered priority (tier 1 always, tier 2 naturally, tier 3 only if it comes up) (PR #1)
- [x] extract.ts — vault paths now entity-type-rooted (item/zara/belt.md); gravity weight system increments each turn (doubles if tier 1 unfilled), flushes at weight≥10; inferred attrs marked *(inferred)* in notes; incomplete nodes get frontmatter flag (PR #1)
- [x] route.ts — extraction schema updated to entity_type + tags[] (tags emerge from attrs, never assigned upfront); buildAgendaContext soft re-anchor (2–3 turn follow + steer back) replaces hard topic-block; dual-signal intent validation (model flag + forward-looking utterance marker) (PR #1)
- [x] Phase 4 — hub node removal: type hub nodes (item/index.md etc.) eliminated; entities write directly to vault; entity_type stored on vault_notes (PR #2)
- [x] Phase 4 — attribute-based edges: linkByTag() creates shared-topic edges (link_type='tag') between conversation nodes; brand→item edges carry link_type='brand' (PR #2)
- [x] Phase 4 — GraphCanvas visual overhaul: TOPIC_COLORS expanded to all 31 topics (fixes gray node bug); ENTITY_TYPE_COLORS added (5 types); entity nodes colored by entity_type, system hubs by topic; system nodes render as hollow rings; tag edges barely-visible white, brand edges faint purple; tooltip shows entity_type (PR #2)
- [x] Supabase schema: vault_notes.entity_type + vault_links.link_type columns added + migrations applied (PR #2)
- [x] Reviewer annotation feature — `[notes in brackets]` in chat input stripped before LLM call, stored raw in Supabase messages for session review; placeholder hint added (session 10)
- [x] Extraction bug fixes (session 10) — brand attr synced to agenda.current.brand when it arrives mid-drill; makeAgendaItem synthesizes Brand attr from entity.brand so getTier1Missing sees it fulfilled; attribute bleed from current entity fixed via prompt CRITICAL rule (new entity introduction → attrs go to entity seed, not global slot)
- [x] Interviewer behavior fixes (session 10) — "I don't know" dead-end rule (rephrase once max, then drop); open-ended wrap-up after 4+ attrs ("anything else?"); casual mention rule (don't pivot immediately on passing mention, finish current drill first)
- [x] Session lifecycle (session 10) — conversations.updated_at auto-trigger (Postgres); 2h timeout detected on opening request → old conversation closed, pending queue + current entity carried to new conversation; incomplete vault_notes injected into system prompt as "Unfinished from last session"; session topic limit (3 entities per conversation) with wrap-up instruction; farewell detection ("Talk soon." etc.) closes conversation immediately
- [x] **Phase 1 — extraction decoupled from chat (session 11)** — `lib/ai/synthesize.ts`: dedicated extraction pass over the last ~8-turn window + current agenda, using **Cerebras gpt-oss-120b (free tier)** with **strict structured outputs** (`response_format: json_schema, strict: true`) so the response is always schema-valid — this structurally eliminates the JSON-leak / attribute-bleed / parse bug class. Chat call now returns **plain text** on the interview path (extraction schema removed from SYSTEM_PROMPT); onboarding keeps a tiny `{reply, onboarding_complete}` JSON contract. `route.ts` `after()` runs `synthesize → extractFacts` async, off the hot path. Dual-signal intent validation moved into synthesize.ts. extractFacts (agenda gravity + vault writes) unchanged. Extraction model is isolated to one fetch block in synthesize.ts — swap to Anthropic Sonnet (or any provider) later by editing that block only. tsc + `pnpm build` clean; **runtime-verified** against live Cerebras (strict schema accepted, valid JSON, correct intent + title/attribute split). **Implements plan `docs/superpowers/specs/2026-06-30-implementation-plan.md` Phase 1.** Uses existing `CEREBRAS_API_KEY` — no new key needed.
- [x] **Extraction integration fixes (session 11)** — `getTier1Missing` made case-insensitive (model emits lowercase attr titles like `category`; tier params are Title Case `Category` — exact match would have read present attrs as missing and blocked vault writes). synthesize.ts prompt: title = bare noun + descriptors/Category go in attributes (was folding "black leather belt" into the title); Title-Case attribute names nudge; require a one-sentence description per entity.
- [x] **Phase 2 — persona rewrite (session 11)** — `SYSTEM_PROMPT` reframed from a covert questioner into a "curious friend who harvests facts from stories." Added the **hard-fact ladder** (harvest from story → infer + soft-confirm → ask once then drop → defer to a later session), **drill depth ∝ user energy/intent** (passing mention = one light question; excited/planning = full drill), a **Rhythm** rule that breaks the relentless one-question cadence (may react or offer a light opinion with no question — never two questions), **cross-fact connection** using vaultContext ("you run mornings — are those shoes for that?"), and **intent = offer value, not probe** ("want me to keep an eye out for deals?"). Softened the two mechanism-leaking spots: `buildAgendaContext` now reads "on your mind right now (a gentle thread, not a checklist)… if it comes up naturally" (was "still need tier 1: X"); `CHECKLIST_PROMPT` (checklists.ts) reworded from "Tier 1 — always capture, unlocks vault writes" to "what's worth knowing… if it surfaces naturally." Deferred re-asks still ride on existing "Unfinished from last session." Implements plan Phase 2. tsc + build clean. **Live-conversation acceptance check still pending.**
- [x] **Phase 3 — graph feels alive (session 11)** — `GraphCanvas.tsx`. **You connected & central**: client-side (never-persisted) `link_type:"self"` edges from `_profile.md` to every top-level entity (items stay nested under brands; everything else links straight to You) → one connected web instead of a floating dot. **One color axis**: every node colored by topic; dropped the system-hollow-ring/entity-filled split and the `ENTITY_TYPE_COLORS` second palette (entity_type now lives in the tooltip only). **Edges visible**: tag `#ffffff08`→`#ffffff1a`, self spine `#ffffff33`, brand `#a78bfa45`. **Node birth**: `seenNodeIdsRef` diffs node IDs across redraws; new nodes scale in (elastic) + one-shot glow pulse (silent on first load and on resize). **Polling**: single blind 4s reload → two fetches (3s + 6.5s), birth-diff makes the second idempotent so slow extractions still surface. Added `Events` topic color; removed stray repo-root `nul` file. `GraphLink.link_type` union gained `"self"`. Implements plan Phase 3. tsc + build clean.
- [x] **Phase 4 — advertiser data layer (session 11)** — Migration `phase4_advertiser_data_layer` applied to Supabase (verified: `intents` table + RLS + `intents: own` policy + partial index on open intents; no new security advisories). **`intents` table** (entity_note_id, category, utterance, confidence, status open|offered|converted|expired, created_at, expires_at) — the CPA/affiliate lifecycle, not a boolean. **`profiles.ad_preferences` JSONB** for per-category consent. **`lib/ads/categories.ts`** static topic→affiliate-category map (`affiliateCategory()`; non-commercial topics → null). **Wired**: `extract.ts` `recordIntent()` fires in `writeEntityToVault` (both branches) when `item.intent` — one open intent row with category + utterance + confidence + 30-day expiry, dedup-guarded against an existing open intent for the same entity. `AgendaItem`/`makeAgendaItem` carry `intent_utterance`/`intent_confidence`. Types: `Profile.ad_preferences`, `Intent`/`IntentStatus`. schema.sql updated. Implements plan Phase 4. tsc + build clean. **Deferred**: life-events-as-first-class typed signal (under-specified, not in acceptance; already flows as `event` entities tagged Life Events — add when ad-targeting consumes it). **Live-conversation acceptance check pending.**
- [x] **Session 12 — extraction-integrity + persona fixes (commit `400fc28`, branch `feat/phase1-4-persona-graph-ads`)** — Reviewed session 11's live conversation (a mall/coffee/shirt/salon chat) against what actually reached the graph; only 2 of ~7 entities wrote cleanly. Five fixes, each verified before moving on (tsc + `pnpm build` clean throughout):
  - **#1 Brand-hub dedup** — brand hubs diverged between `item/<brand>.md` (when hosting an item) and `brand/<brand>.md` (standalone), producing **two Starbucks nodes**. Unified into `upsertBrandHub()` → always `brand/<slug>.md`; `UNIQUE(user_id,path)` now makes a duplicate structurally impossible. Append-merges description instead of clobbering. *Verified by construction.*
  - **#2 Hybrid session-end persistence** — new exported `closeSession()` in extract.ts: banks **intent-bearing** entities to the vault as incomplete nodes (fires `recordIntent` — the revenue signal) and returns **non-intent** survivors to carry forward. Unifies the farewell + 2h-timeout close paths. `route.ts` opening block now carries survivors forward from a **farewell-completed** conversation too (previously only stale-active). *Verified against live DB.*
  - **#4 Title upgrade** — `applyNameAsTitle()` adopts a captured **Name** attribute as the title for place/person entities, called at the single `writeEntityToVault` chokepoint (mutates the shared agenda item too). Generic "Mall" → `place/galeria-rzeszow.md`, no orphan. *Verified against live DB.*
  - **#3 Late-fact merge (the "Mall" bug)** — facts stated **after** an entity flushed (Frequency/Location) were lost because attributes only ever merged into `agenda.current` and re-emitted known entities were skipped/duplicated. Now: extracted entities matching current / pending / an already-flushed vault node (case-insensitive title) **merge** instead of skip. `parseNote()` recovers a node's attrs from content_md; `nodeToAgendaItem()` folds in late facts and re-runs `writeEntityToVault` (upsert by path), recomputing the incomplete flag. **Evidence-driven** (systematic-debugging): replayed the real turns through `synthesize` — model correctly surfaces late facts (as attributes when entity is current, as a re-emitted entity once flushed); the **plumbing** was the bug. *Verified against live DB.* Depends on #4 for stable titles, so #4 was done first.
  - **#5 Interviewer persona** (`SYSTEM_PROMPT` in route.ts) — addresses the 4 reviewer `[bracket]` notes from the transcript: added a **Transitions** rule (bridge between engaged threads — "glad you found a barber you like, back to the mall…" — silent pivot stays for deflections only); **confirm-before-ending** (propose wrap as a question, wait for the user, don't declare the session over); **warm personalized sign-off** (always contains "Talk soon" + a touch that shows listening). Farewell detector narrowed `/\btalk soon\b/i` so wrap *proposals* don't close the session prematurely (gate checked 5/5). **#5 conversational behavior is LIVE-TEST PENDING** — prose can't be unit-tested.
  - Also **hardened `/hypereset`** (`.claude/skills/hypereset/SKILL.md`) to clear `intents` + `messages` + `conversations` (was leaving stale message history and the Phase 4 `intents` table untouched; pending was already cleared).
  - **Still to live-test (next session):** (a) #5's actual phrasing behavior; (b) the full **farewell → reload → carryover** loop end-to-end (closeSession + opening-read were verified separately, not driven through two live requests). Test plan: `/hypereset` → one conversation: mall → name it → drop frequency/location a few turns later → say you "need" a shirt → say bye. Watch the graph; confirm single brand nodes, the named mall completing, the shirt intent banked, and a warm confirm-then-sign-off.
  - **Note:** session 12's DB-level verification used an ad-hoc `npx tsx` harness (load `.env.local`, dynamic-import via `pathToFileURL`, insert a conversation with a synthetic agenda, call the real exported fn, assert + clean up). Those scratch files do **not** persist across sessions — recreate if needed. There is no committed test runner in the project.
- [x] **Session 13 — clean-slate extraction reproduction verified (commit `52256cd`, `main`)** — Re-ran the session-12 fixes against a fresh `/hypereset` conversation (mall → name it → pants + plant purchase intents → stores → second mall). This time the correct graph reproduced **automatically, no manual backfill**: single brand/place node per entity, both intents banked to `intents` (fashion/pants + home-kitchen/plant), store↔mall + item↔store relation edges auto-created. Five fixes committed (tsc + self-checks clean):
  - **Intent markers broadened** (`synthesize.ts INTENT_MARKERS`) — added "looking to", "thinking of/about", "planning on", "gonna get/grab", "hoping to", "would like", "in the market", "shopping for", "pick up". Root cause of the dropped plant/pants intent: the gate's phrase list was too literal ("looking to get" ≠ "looking for"). Gemini flags intent correctly; the marker gate was silently downgrading it.
  - **Blank-value attrs filtered** at the synthesize boundary (`keepAttr`) — an empty `Name` no longer counts toward a place's tier-1, which had been flushing an unnamed place as a duplicate node.
  - **Placeholder Name dropped** (`keepAttr` + `PLACEHOLDER_NAME`) — a generic value ("another mall", "the store", bare "mall") is not adopted as a Name, so the place stays **incomplete** and the interviewer circles back for the real name. Regex is Name-only and whitelists the article form to generic place-nouns, so real names ("the Louvre", "Galeria Rzeszow") survive.
  - **Deterministic intent-item title** (`extract.ts writeEntityToVault`) — a purchase-intent item gets a code-derived `New ` prefix (`New pants` → `item/new-pants.md`), independent of the model; `extractFacts` aliases the bare noun in `noteByTitle` so a re-emitted "pants" re-opens the same node instead of duplicating. (Graph-structure decision: intent baked into the title.)
  - **Relation prompt patterns** (`synthesize.ts SYSTEM`) — brand↔place ("at") and item↔store ("shop at") edges now emit; wires stores to the mall they're in and shopping-intent items to the stores they'll be found in.
  - **Deferred:** narrowing the item→place relation (New pants picked up a stray link to the mall, not just the stores) — low-priority edge noise, left for later per owner.
- [x] **Session 14 — session-review fixes + ground-layer profile capture (commit `03b8683`, `main`)** — Reviewed the 2026-07-03 Germany-trip conversation against the graph. The trip decomposed cleanly (event + place + person + flight-tickets) but the interviewer wrapped after ~5 exchanges and never circled back to unfinished threads; work was dropped entirely. Four fixes committed (tsc + self-checks + build clean; migration applied):
  - **#1 Premature wrap-up** (`route.ts`) — root cause: `sessionTopicCount` counted rows in the `extractions` table (one per note written, inflated by brand hubs + every upsert re-insert → **9 rows for 6 turns**), and the "≥3 entities" gate tripped after ~2 turns. Now measures **user turns** (`messages.filter(user).length`); proposes wrap at **≥10 turns**. Dropped the dead `extractions` count query.
  - **#2 Unfinished threads never resurfaced** (`synthesize.ts` + `route.ts`) — the circle-back only fired on `incomplete: true` notes. Legacy/leaked placeholder-titled nodes ("another mall") have no such flag, so they orphaned forever. Exported `isPlaceholderName`; the `incompleteThreads` filter now also surfaces place/person/org nodes whose title is a placeholder → "still need its real name." **Self-heals stale junk on the user's next chat, no `/hypereset` needed.**
  - **#3 Job/work had no home** (`entityTypes.ts`, `synthesize.ts`, `extract.ts`, `route.ts`) — added a 6th entity type **`org`** (employer/workplace/school/team/club; tier1 = Name + Role). Flows automatically into the strict JSON schema enum + `EntityType` + `database.ts`. Extraction prompt routes work/job/career/school → org; `applyNameAsTitle` now adopts a captured Name for org (so `org/acme.md`, not `org/my-job.md`); interviewer `SYSTEM_PROMPT` gained a rule to **stop deflecting work openings** ("anything fun *outside* work?"). Org tags `Work` — GraphCanvas color already exists.
  - **#4 Ground-layer profile capture** (`profiles.base_profile` JSONB migration + all 4 files) — base demographics that anchor ad-targeting (occupation, where they live, age). Occupation reuses the new `org` node (no new storage); **age + home_location** land on new `profiles.base_profile` JSONB. `synthesize.ts` emits top-level `user_age` / `user_home_location` as **user-level facts routed away from the graph**; `extractFacts` read-merge-writes them (no clobber). `route.ts` computes missing base slots and injects **one** gentle, **permission-first** question **only in a lull** (`!agenda.current && !pending.length`); frequency is left to the model's soft judgment (ponytail-commented hard-gate upgrade path: once-per-session + a `declined[]` list). **Physical characteristics deliberately excluded** — trust cost too high; clothing size still rides the natural item `Size` attribute. Migration `base_profile_demographics` applied + verified (`{}` default).
  - **All #1–4 conversational/model behavior is LIVE-TEST PENDING** (prose + model output can't be unit-tested). Test plan: `/hypereset` → one chat that opens with your job → runs ~10 turns → mentions an unnamed place → hits a lull. Confirm: work lands as an `org/` node, `base_profile.age`/`home_location` save, no early wrap, the placeholder circles back, and the base question fires **only** in the quiet moment.
- [x] **Session 15 — session-review fixes: interest type + org/persona tightening (`main`)** — Reviewed the 2026-07-03 work/mall conversation (`org/welodata.md` captured cleanly, wrap/sign-off correct) against the graph. Three fixes committed (tsc + build + both self-checks clean):
  - **#1 Life-event hallucination** (`route.ts` `SYSTEM_PROMPT`) — "just started working" → AI replied "congrats on the new job!" (user flagged it inline). Added a **mirror-only rule**: never assume/congratulate an unstated life event; ask before reacting to an invented version.
  - **#2 Org over-classification** (`synthesize.ts` `SYSTEM`) — "my background is in software development" wrongly spawned `org/software-development.md`. Tightened org to a **specific named organization**; a bare skill/field/discipline is NOT an org → routes to `interest` or drops.
  - **#3 New `interest` entity type** (7th type) — study interests ("financial investment", "AI knowledge") were mis-typed as `item` (nonsensical brand/category drill). Added `interest` with **tier1 = []** (complete on naming, writes to graph immediately, no drill-down, can grow sub-nodes later). Edits: `entityTypes.ts` (ENTITY_TYPES + TIER_PARAMS + self-check), `synthesize.ts` (routing + required-attrs), `extract.ts` + `database.ts` unions (also added the **missing `org`** to `database.ts` — a latent session-14 inconsistency). Colors by topic tag — GraphCanvas unchanged.
  - **#4 Stray brand→place link** (Zara double-linked to a stale cross-session mall) — **deferred**: cross-session edge case, masked by `/hypereset`, same root as the deferred session-13 item→place over-linking.
  - **All #1–3 are prose/model-behavior → LIVE-TEST PENDING.** Test: `/hypereset` (clears accumulated cruft: bad `org/software-development`, mis-typed interest items, stray link) → chat opening with work + a couple of interests. Confirm: employer → `org/`, interests → `interest/` (not `item/`), no invented "new job" reaction.
- [x] **Session 16 — landing page (build + spec) + docs restructure (`main`)** — First marketing page at `/`, brainstormed → spec'd → built with the frontend-design skill; also graph-node cleanup and a CLAUDE.md/CHANGELOG.md split.
  - **Landing page design spec** (`docs/superpowers/specs/2026-07-03-landing-page-design.md`, commits `3a7478c`/`6bc79f4`/`124c408`) — Revolut-style dark cinematic direction (inspiration: klarna.com, revolut.com); positioning reframed to **personal AI assistant that's radically honest about data**, not an ad platform. Six benefit-first sections; consent-only ads promoted to the **flagship §2** (category claim: "the first hyper-tailored personal assistant with consent-only ads") with a hero claim badge. Added proactive-notifications + app-download (coming-soon, honest — app not built) sections.
  - **Landing page build** (commit `fb01e16`) — `app/page.tsx` renders `<Landing/>` (logged-in still → `/graph`). New `components/marketing/`: `Landing.tsx` (server composition), `DemoGraph.tsx` (self-contained ambient D3 graph — deliberately NOT GraphCanvas: no zoom scroll-hijack, no Supabase, viewBox-responsive, reduced-motion aware; reuses the app's topic-color language), `graphData.ts` (seeded demo — renamed from `demoGraph.ts` to dodge a Windows case-clash with `DemoGraph.tsx`), `ConsentPanel.tsx` (**signature**: offer card appears only when a consent toggle is on — consent-only ads *demonstrated*), `TalkDemo.tsx` (chat→node-birth split scene), `GrowthTimeline.tsx` (week-1→8 scrubber), `Nav.tsx` (sticky blurred), `Reveal.tsx` (scroll-reveal). Fonts: **Space Grotesk + Inter** via `next/font` (`--font-space-grotesk`/`--font-inter` → `font-display`/`font-body` theme tokens); scroll-reveal + hero float keyframes in `globals.css`. `pnpm build` + tsc clean; `/` serves 200 with content. **Visual polish is user-feedback pending** ("good start, room for improvement") — open items: hero legibility (graph behind headline), consent-toggle feel, exact "consent-only ads" phrasing.
  - **Graph-node cleanup (no `/hypereset`, preserves over-time test history)** — retyped 3 nodes to the new `interest` type (`financial-investment`, `ai-knowledge`, `software-development` — was `org`); merged duplicate mall nodes (`another-mall` → `millenium-mall`); corrected store→mall edges from yesterday's mis-extraction (Galeria Rzeszow = H&M + Half Price; Millenium = TK-Max + Zara). Confirmed in-place retyping is safe (title-based dedup).
  - **Docs restructure** (commit `fcd9903`) — split the append-only session log out of CLAUDE.md into **CHANGELOG.md** (byte-for-byte, verified identical); CLAUDE.md now holds only evergreen context/rules/focus (235→154 lines, cheaper to load each session). `/hypesave` updated to append here, not there.
- [x] **Session 17 — `located_in` place nesting + `/hype-review` skill + skill renames (`main`)** — Reviewed the 2026-07-03 Rzeszów/Germany conversation and acted on the geography question it raised. No app-behavior regressions; tsc clean.
  - **New `/hype-review` skill** (`.claude/skills/hype-review/`) — pulls the latest conversation's messages via Supabase MCP and critiques the interviewer's turns against the "Key interviewer rules" in CLAUDE.md (value rule, natural grouping, deflection, agenda anchoring, wrap triggers, persona). Used it this session; the review flagged a soft deflection re-probe (kept asking what he hoped to buy after "just browsing") and one early wrap offer — both minor.
  - **Skill renames** — `hypesave`/`hypereset`/`hypereview` → `hype-save`/`hype-reset`/`hype-review` (dirs moved, `name:` frontmatter updated, two `/hypereset` refs in CLAUDE.md fixed).
  - **`located_in` containment relation wired** — places now nest geographically (venue → city → country) instead of floating as flat siblings. **Direction:** the extractor emits containment smaller→larger ("Grand Club **in** Rzeszów"); a new `located_in` link_type nests **source under target** (opposite of `brand`/`relation`, which nest target under source), so the smaller place becomes the child. Edits: `database.ts` (both link-type unions), `extract.ts` (containment labels `in`/`located in`/`inside`/`within`/`part of` → `located_in`), `GraphCanvas.tsx` (`located_in` is structural via a `childId` helper; soft-cyan `#67e8f955` edge; hover label surfaced), `synthesize.ts` (relations prompt gained a place-containment pattern + an EXCEPTION letting `in` target an already-tracked home city/country even when it isn't a fresh entity — the resolver already matches all conversation notes, so cross-window edges self-heal). **Backfilled** the current vault: You → Poland → Rzeszów → {Galeria, Millenium Mall, Grand Club}, Giessen → Germany. Confirmed `base_profile.home_location = "Rzeszow"` was already captured correctly (self-fact, not a graph node) — the `place/rzeszow.md` node is a separate, legitimate entity that the containment tree now hangs off.
  - **Deferred (no code):** softer node-palette exploration — built a comparison artifact (Current vs Muted/Pastel/Dusk, HSL-derived), owner set it aside for a later session; `TOPIC_COLORS` unchanged.
- [x] **Session 18 — review pre-deploy hygiene + chat-history restore + user-menu plan (`main`)** — Actioned five of the 2026-07-03 review findings, then assessed + planned the user menu (finding #2). tsc clean; `pnpm install` clean.
  - **#5 chat-history restore on reload** — a reload mounted `ChatPanel` with empty history, so the model generated a fresh opener mid-conversation (amnesia bug). `graph/page.tsx` now loads the **active (<2h)** conversation's messages (mirrors the reuse rule in `route.ts`) → `GraphWrapper` → `ChatPanel` seeds prior turns for model context and shows the last AI line where we left off. **Visual half deliberately skipped** (one message at a time, no scrollback). Older sessions are NOT reloaded — they already feed back through the vault, and re-injecting raw transcripts would double-count facts + bloat the window. **Live-test pending:** reload mid-chat → AI resumes, not a cold opener; older-than-2h → fresh opener.
  - **#7 missing catch in `send()`** (`ChatPanel.tsx`) — a network failure / bad JSON was an unhandled rejection + silent stall; added a `catch` that surfaces a retry message.
  - **#8 `import "server-only"` in `admin.ts`** — the service-role client now fails the build if it's ever pulled into a client bundle (was only a naming convention before).
  - **#9 dead deps removed** — dropped `groq-sdk` + `@anthropic-ai/sdk` from `apps/web/package.json` (zero source imports — both AI paths run on Gemini/Cerebras via fetch); `pnpm install` updated the lockfile.
  - **#10 / schema drift** — the "missing index" finding was **stale**: the partial index `vault_notes(user_id, scheduled_for) WHERE scheduled_for IS NOT NULL` already exists live. Real gap was doc drift — `intent` + `scheduled_for` columns + that index existed in the live DB but were missing from `schema.sql` (a fresh build would break every chat turn). Synced `schema.sql`. Also fixed stale `GROQ_API_KEY` → Gemini/Cerebras env docs in `CLAUDE.md`.
  - **Deferred per owner:** #1 affiliate/revenue loop + #4 consent capture → until model performance is confirmed; #6 rate limits on `/api/chat` → after testing. #3 retention loop flagged **important** for product strategy.
  - **User-menu assessment + plan (finding #2)** — three-model workflow: Fable did the high-level UX assessment, Opus wrote the plan, Sonnet to execute later. Saved to `docs/superpowers/specs/2026-07-04-user-menu.md`. Shape: ghosted avatar chip top-right → right-side slide-over drawer, sub-views push inside the same drawer (no route changes). **Slice 1 (pre-deploy blocker — app has NO logout at all today):** drawer shell + Logout (`signOut()` + redirect; middleware already guards `/graph`) + lean Profile (display_name/age/home → RLS-scoped browser-client write, no API route). **Slice 2:** Manage nodes (list newest-first, entity-type chips + search, read-only view, delete with link-count confirm → `vault_links` cascade cleans edges → bump `refreshTrigger`; hide `_profile.md`; block delete of `source="system"` brand hubs) + Export (`.zip` of vault markdown). **Slice 3 deferred:** node edit, upcoming-events retention, real account deletion, clickable nodes. **Owner correction:** NO ad-consent toggle — consent is per-moment, asked inline by the interviewer when a deal is available ("new deal on those shoes — want a look?"); `ad_preferences` stays UI-untouched. **Open decisions:** export `.zip` (adds jszip) vs single `.md`; pre-deploy account deletion mailto-stopgap vs slice 3.
- [x] **Session 19 — user menu built (Slices 1 + 2 + graph settings) (`main`)** — Executed the session-18 user-menu plan and added a graph-personalization view on top. tsc + `pnpm build` clean throughout; live UI drive still owner-pending.
  - **Slice 1 — shell + logout + lean profile (pre-deploy blocker; app had NO logout anywhere)** — New `components/menu/UserMenu.tsx` (client): ghosted top-right avatar chip (first initial) → right slide-over drawer (`z-50`, above `z-30` chat; full-screen on mobile; Esc / click-outside close). Root rows + a **Profile** sub-view editing `display_name`, `age`, `home_location` → RLS-scoped browser-client write (`profiles.update`, no API route). **Logout** = `supabase.auth.signOut()` → `router.push("/login")` (middleware already guards `/graph`). `graph/page.tsx` adds a `profiles` select to the existing `Promise.all`; `GraphWrapper` mounts the menu. **Added Gender** to the profile (owner request) — select with Female/Male/Non-binary/Other/Prefer-not-to-say, stored in the same `base_profile` JSONB (no migration; `database.ts` type extended with `gender?`).
  - **Slice 2 — Manage nodes + Export** — Manage-nodes sub-view fetches `vault_notes` via browser client (RLS, newest-first, `_profile.md` hidden), with a text search + 7 entity-type filter chips; row tap → read-only raw markdown; **Delete** shows a `vault_links` link-count confirm → `vault_notes.delete()` (edges cascade at the DB level) → optimistic list removal + bumps `refreshTrigger` so the canvas drops the node. **System nodes (`source="system"` brand hubs) show "can't be deleted"** instead of a delete button (deleting one orphans its items). **Export** = new server route `app/api/vault/export/route.ts` (RLS user client) → **jszip** zips every note at its real `path` → `hype-vault.zip`, a drop-in Obsidian vault (owner picked `.zip` over single `.md`). **Delete account** row added as a **disabled placeholder** (no action) — real deletion + privacy/legal copy deferred to pre-deploy; owner declined the mailto stopgap.
  - **Graph settings (new, beyond the plan)** — owner asked for graph personalization. New `lib/graph/palettes.ts` is now the single source of truth for node color: the base topic→hex map moved out of `GraphCanvas.tsx`, plus **4 generative palette modes** (`vibrant`/`pastel`/`neon`/`mono` via `d3.hsl` transforms — no hand-authored alternate maps; Profile-white and greys pass through untouched) and **4 dark background swatches**, persisted to **localStorage** (`hype:graph-settings`, per-browser, zero backend — owner picked this over a `profiles` JSONB column). `GraphCanvas` takes a `settings` prop → `nodeColorFor(topic, palette)`, SVG background from `settings.background`, `palette` added to the draw-effect deps so a mode switch recolors live (300ms transition). `GraphWrapper` holds `settings`, loads from localStorage after mount (avoids SSR hydration mismatch), passes to canvas + menu; menu **Graph settings** sub-view has palette buttons + background swatches, applying live to the graph behind the translucent drawer. **Deferred (slice B):** per-topic custom colors, custom-hex/light background (node palette is dark-tuned), link-color + node-size/physics controls, re-extraction tombstone on node delete (a deleted node can still be re-created by the agenda next conversation).
- [x] **Session 20 — Mobile app (Expo): scaffold → on-device core loop verified (`main`)** — Built the Android app from the `docs/mobile/2026-07-06-mobile-app-plan.md` plan and ran the full core loop on a physical device: login → live graph → chat interviewer → new nodes created. Monorepo now ships web + mobile from one codebase (same later goes to iOS via EAS Build; owner is Windows-only). tsc clean throughout; `expo-doctor` 20/20.
  - **New `packages/shared`** — extracted the pure graph layer (types + `graph-layout.ts` `withSelfLinks`/`buildDegreeMap`/`nodeRadius`/`synthesizeSelfLinks` + `palette.ts` `nodeColorFor`/`DEFAULT_SETTINGS`) so web and mobile compute layout/colors identically; web re-imports it (no regressions). pnpm `nodeLinker: hoisted` (RN/Expo autolinking needs deps at the workspace root), react pinned `19.2.4` to dedupe.
  - **New `apps/mobile`** (Expo SDK 57, RN 0.86, Expo Router) — `lib/supabase.ts` (supabase-js + AsyncStorage + Bearer JWT, not `@supabase/ssr`), login/index/`_layout` (GestureHandlerRootView), auth-gated `graph.tsx`.
  - **Phase 3 — bearer auth on `/api/chat`** — web `lib/supabase/server.ts` gains `createBearerClient(token)` (JWT rides every PostgREST request → RLS still scopes to the user); `route.ts` detects `Authorization: Bearer` (mobile) vs cookie (web), validates via `getUser(token)`. Cookie path untouched. Verified: bogus/no token → 401.
  - **Phase 4 — `GraphCanvasSkia`** — @shopify/react-native-skia render (Canvas/Group/Circle/Line/Text, `matchFont` system font, 6-digit hex + `opacity` prop), d3-force settled once (`sim.tick(300)` in a `useMemo`), pan + pinch-zoom on reanimated shared values → `useDerivedValue` transform (gestures stay on the UI thread). Only the render is new; layout math reused from `@hype/shared`. **Deferred:** per-node drag, node-birth animation, focal (vs center) zoom.
  - **Phase 5 — live data** — `graph.tsx` fetches `vault_notes`/`vault_links` (same select as web `graph/page.tsx`), maps → `GraphData`, feeds the canvas; loading/error-retry/empty states. Skipped `parseAttributes`/`wordCount` (canvas draws only topic/title/degree). Deleted the Phase-4 fixture.
  - **Phase 6 — `ChatPanel`** (RN) — ports the web chat contract: AI line top, single bottom input (one message, no scrollback), reused word-by-word typewriter. `postChat()` attaches `session.access_token` as `Authorization: Bearer` to `POST {EXPO_PUBLIC_API_URL}/api/chat`; opener on mount; rate-limit/server errors surfaced. `onReply` → poll `load()` at **3s + 6.5s** (extraction is fire-and-forget `after()` server-side — same poll-twice as web). **Deferred:** ad `card` render, `<2h` session restore, node-birth animation.
  - **Phase 7 — EAS dev-client build + on-device** — `expo-dev-client ~57.0.5`, `eas.json` (`development`/`preview`/`production`), `app.json` name→"Hype"/slug→"hype"/`package`+`bundleIdentifier` `com.joshuamendella.hype` (provisional — renamable until first store submit)/dark UI; `eas init` linked project `49fdea16-…` (owner `josh_mendel`). **EAS + Windows symlink fix:** first upload failed `EPERM: symlink` on `.agents`/`.claude` (Claude tooling dirs symlinked on disk) → added root **`.easignore`** (used *instead of* `.gitignore` when present, so it mirrors gitignore + excludes `.agents/`,`.claude/`,`docs/`); re-upload 1.3 MB. Dev-profile needs no EAS env — a dev build loads JS from local Metro, which reads `apps/mobile/.env`. Built APK, sideloaded, ran the full loop on-device. **Next: polish punch list** (owner: "went well, some points to improve" — captured tomorrow), then Phase 8 standalone preview APK (needs `EXPO_PUBLIC_*` as EAS env + a deployed web URL).
  - **Also swept into this commit** (previously-uncommitted working-tree changes, not this session's mobile work): landing-page polish across `components/marketing/*` + `globals.css` (new `PhoneMock`, `ThemeToggle`), `AdCard.tsx`, business/GTM docs (`BUSINESS.md`, `docs/gtm/`, `HYPE_BUSINESS_ASSESSMENT.md` edits), and `schema.sql`/spec touch-ups.
