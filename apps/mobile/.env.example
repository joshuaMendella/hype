# Copy to .env and fill in. EXPO_PUBLIC_* are inlined into the app bundle (client-safe values only).
# URL + anon key are the same public values as apps/web (.env.local NEXT_PUBLIC_*).
EXPO_PUBLIC_SUPABASE_URL=https://aykjvvtolkaqvijfeewn.supabase.co
EXPO_PUBLIC_SUPABASE_ANON_KEY=
# Your PC's LAN IP running `pnpm dev` in apps/web, e.g. http://192.168.1.20:3000
EXPO_PUBLIC_API_URL=http://localhost:3000
